import { createElement } from 'react';

import { ProjectPageView } from './project-page-view';

const ProjectPage = () => createElement(ProjectPageView);

// eslint-disable-next-line import/no-default-export
export default ProjectPage;
